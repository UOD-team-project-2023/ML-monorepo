# installation

- create .venv (python -m venv ./venv)
- activate the venv (./venv/Scripts/activate)
- pip install -r requirements.txt
- update SERVER_URL in env_TEMPLATE then rename to .env
- called main.py

To quit use the system tray icon